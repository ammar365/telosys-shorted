${SHARP} ---
${SHARP} Created on $today.date ( Time $today.time )
${SHARP} Generated by $generator.name ( version $generator.version )
${SHARP} ---

${SHARP} Database configuration  
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/xe
spring.datasource.username=amm
spring.datasource.password=amm
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

${SHARP} -- dont use this it will drop all tables
${SHARP} spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

server.port=${REST_SERVER_PORT}
